# add (adicional), update (atualiza), clear, discard
# union | (une)
# intersection & (todos os elementos presentes nos dois sets)
# difference - (elementos apenas no set da esquerda)
# symmetric_difference ^ (elementos que estão nos dois sets, mas não em ambos)

# sets = conjuntos não possuem par de chave e valor, somente valores. 
# não tem indice, não pode acessar um valor especifico dos valores
# set não aceita os elementos repetidos, caso tiver ele não mostra no print

# s1 = {1,2,3,4,5,6} 

""" s1 = set() # -> para criar set vazio.

s1 = set()
s1.add(8) # -> para adicionar o valor ao conjunto
s1.add(6)

# s1.discard(8) # -> para discartar o valor 8 do conjunto

s1.update('chayene') # -> itera sobre cada letra
print(s1) """

s1 = {1,2,3,4,5,8}
s2 = {1,2,3,4,5,6}
# s3 = s1 | s2 # -> união dos elementos
# s3 = s1 & s2 # -> todos os elementos que estão nos dois sets
# s3 = s1 - s2 # -> todos os elementos que estão apenas no set da esquerda
# s3 = s1 ^ s2 # -> elementos que estão nos dois sets, mas não em ambos



print(s3)


